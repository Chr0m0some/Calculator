/* Operations */
function add(operator, operand) {return operator + operand}; 
function subtract(operator, operand) {return operator - operand};
function multiply(operator, operand) {return operator * operand};
function divide(operator, operand) {return (operand === 0) ? alert('Woah buddy...') : operator / operand};

